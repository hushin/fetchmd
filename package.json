{
  "name": "mdfetcher",
  "version": "0.1.4",
  "module": "index.ts",
  "type": "module",
  "bin": {
    "mdfetcher": "index.ts"
  },
  "scripts": {
    "build": "bun build ./index.ts --compile --outfile mdfetcher",
    "version": "git add -A",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/jsdom": "^21.1.7",
    "@types/turndown": "^5.0.5"
  },
  "peerDependencies": {
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "commander": "^13.1.0",
    "jsdom": "^26.0.0",
    "turndown": "^7.2.0"
  }
}
